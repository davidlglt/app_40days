<%= simple_form_for([course, review]) do |f| %>
  <div id=""></div>
  <%= f.input :comment %>
  <%= f.input :star %>

  <%= f.hidden_field :course_id, :value => @course.id %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<div class="container text-center">
  <div class="row">
    <div class="col-sm-offset-4 col-sm-4">
      <h1>Vous devez vous créer un compte</h1>

      <%= link_to omniauth_authorize_path(resource_name, :facebook), class: "btn btn-default btn-facebook" do %>
        <i class="fa fa-facebook"></i> Sign in with Facebook
      <% end %>
   </div>
  </div>
</div>

<div class="container text-center">
  <!-- Word & Traduciton -->
  <div class="row">
    <p><%= @word.word %></p>
    <p><%= @word.traduction %></p>
  </div>
  <!-- Validation Button -->
  <% @answers.each_with_index do |answer, index| %>
      <div class="jenesaispas">
        <%= link_to answer, course_update_word_score_path(@course, @word, level: index), class: "btn btn-success" %>
      </div>
  <% end %>
</div>

